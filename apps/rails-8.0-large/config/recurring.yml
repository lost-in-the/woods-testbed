# Extraction fixture; run bin/rails testbed:drain locally (no scheduler dependency).
production:
  publish_due_articles:
    class: PublishDueArticlesJob
    schedule: "*/5 * * * *"
  renew_subscriptions:
    class: RenewSubscriptionsJob
    schedule: "0 * * * *"
