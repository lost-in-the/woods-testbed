<p><% if params[:page].to_i > 1 %><%= link_to '← Previous', request.query_parameters.merge(page: params[:page].to_i - 1) %> · <% end %><% if size == 25 %><%= link_to 'Next →', request.query_parameters.merge(page: [params[:page].to_i, 1].max + 1) %><% end %></p>
